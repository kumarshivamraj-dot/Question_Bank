# PDF Processing Dependencies
pdf2image==1.17.0           # Converts PDF to images (uses Poppler)
pytesseract==0.3.10         # Python wrapper for Tesseract OCR
Pillow>=11.0.0,<12          # Image manipulation; newer wheels support Python 3.13
opencv-python>=4.10.0.84    # Newer wheels are safer on Python 3.13
PyPDF2==3.0.1               # Read PDF metadata (page count)
PyMuPDF==1.25.3             # Native PDF text extraction + page rendering
python-pptx==0.6.23         # Extract class PPT/PPTX text
numpy>=2.1.0,<3             # Required by OpenCV; newer wheels support Python 3.13

# Queue & Messaging
redis==5.0.1                # Redis client for queue

# File Watching
watchdog==4.0.0             # Monitor folder for new PDFs

# Configuration & Environment
python-dotenv==1.0.1        # Load .env files
fastapi==0.116.1            # Web API + UI
uvicorn==0.35.0             # ASGI server
python-multipart==0.0.20    # File uploads for FastAPI

# Data Validation
pydantic==2.6.1             # Data validation and models

# Utilities
python-dateutil==2.8.2      # Date/time utilities


# Regex and ollamas 
regex>=2023.10.3
requests
sqlite-utils
anthropic>=0.39.0
